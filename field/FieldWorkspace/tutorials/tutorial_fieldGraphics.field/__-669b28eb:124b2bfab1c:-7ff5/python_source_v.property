string>
# this makes a new canvas ÑÊa fullscreen place to draw using Field's graphics system
# obviously, you only have to do this once
canvas = getFullscreenCanvas()

# if you wanted a window you could do something like:
# canvas = getWindowedCanvas(bounds=(30,50,100,200))
