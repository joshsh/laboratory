string>
# this box starts the opencv system from the hypermedia libraries

from hypermedia.video import *

opencv = OpenCV(p);

def nothing(): pass

def stop():
	opencv.stop()

# we do nothing until we stop
_r = nothing, nothing, stop
